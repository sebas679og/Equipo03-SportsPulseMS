# API-Football mock
sports-pulse.api.base-url=http://localhost:8090
sports-pulse.api.key=test-api-key

# Auth service mock
auth.service.url=http://localhost:8091
auth.service.name=ms-auth

# Security
app.security.internal-api-key=test-internal-key

# JWT
sports-pulse.jwt.secret=test-secret